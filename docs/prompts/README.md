# Prompt registry requirements

Prompts are versioned artifacts associated with an allowed task, strict output schema, redaction policy, model allowlist, token/cost ceiling, evaluation set, approval state, deterministic fallback, and retirement date. No prompt grants tool authority. Production prompt bodies and approved examples are intentionally absent from this architecture phase and must not contain restricted PII.
