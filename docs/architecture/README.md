# Architecture index

The accepted style is a modular monolith with clean/hexagonal boundaries. Read the system context, module dependency rules, state machine, scheduling/payment sagas, and ADRs before implementation. Discovery facts are separated from target decisions so unverified assumptions remain visible.
