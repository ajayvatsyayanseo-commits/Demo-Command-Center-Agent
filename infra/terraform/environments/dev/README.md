# Development environment

Use isolated state/data/secrets and conservative small capacity. Provider effects remain sandbox/disabled. Supply `terraform.tfvars` outside source control and compose root modules after account discovery.
